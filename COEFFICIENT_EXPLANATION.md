# Как работает коэффициент (коэф) и цена ставки

## Откуда берется коэффициент?

Коэффициент приходит **от парсера** в поле `coef` (например: `"2.50"`, `"1.85"`, `"3.20"`).

Это стандартный коэффициент букмекера, который означает:
- Коэффициент **2.50** = если поставить 1 доллар, получишь 2.50 доллара (прибыль 1.50)
- Коэффициент **1.85** = если поставить 1 доллар, получишь 1.85 доллара (прибыль 0.85)
- Коэффициент **3.20** = если поставить 1 доллар, получишь 3.20 доллара (прибыль 2.20)

## Как коэффициент конвертируется в цену для Polymarket?

### Формула конвертации:

```
Вероятность = 1 / Коэффициент
Цена = Вероятность (от 0.01 до 0.99)
```

### Примеры:

1. **Коэффициент 2.50:**
   - Вероятность = 1 / 2.50 = **0.40** (40%)
   - Цена для Polymarket = **0.40**

2. **Коэффициент 1.85:**
   - Вероятность = 1 / 1.85 = **0.54** (54%)
   - Цена для Polymarket = **0.54**

3. **Коэффициент 3.20:**
   - Вероятность = 1 / 3.20 = **0.31** (31%)
   - Цена для Polymarket = **0.31**

### Формат цен в Polymarket:

- **Диапазон:** от **0.01** до **0.99**
- **0.01** = 1% вероятность (самая низкая цена)
- **0.50** = 50% вероятность (середина)
- **0.99** = 99% вероятность (самая высокая цена)

## Код конвертации

В файле `app/services/data_mapper.py`:

```python
def coef_to_probability(self, coef: str) -> float:
    """Конвертация коэффициента в вероятность (0-1)"""
    try:
        coef_float = float(coef)
        if coef_float > 0:
            return 1.0 / coef_float  # Формула: 1 / коэффициент
        return 0.5
    except (ValueError, TypeError):
        return 0.5

def coef_to_price(self, coef: str) -> float:
    """Конвертация коэффициента в цену для Polymarket"""
    probability = self.coef_to_probability(coef)
    # Ограничиваем диапазон 0.01 - 0.99
    price = max(0.01, min(0.99, probability))
    return round(price, 2)
```

## Что происходит на практике?

### Пример ставки:

Парсер отправляет:
```json
{
    "coef": "2.50",
    "homeTeam": "Team 1",
    "awayTeam": "Team 2",
    ...
}
```

Бот:
1. Получает коэффициент: **2.50**
2. Конвертирует: 1 / 2.50 = **0.40**
3. Использует цену **0.40** для размещения ордера на Polymarket

### Размер ставки:

- Установлен в настройках: **2.0 USD** (default_order_size)
- Это размер ставки в долларах
- Если парсер передает `stake`, используется значение от парсера

## Важно понимать:

### Разница между коэффициентом и ценой:

- **Коэффициент** (от парсера):
  - Формат: `2.50`, `1.85`, `3.20`
  - Чем выше коэффициент, тем меньше вероятность
  - Коэффициент 2.00 = 50% вероятность

- **Цена** (для Polymarket):
  - Формат: от `0.01` до `0.99`
  - Это прямая вероятность
  - Цена 0.50 = 50% вероятность

### Сравнение:

| Коэффициент | Вероятность | Цена Polymarket |
|------------|-------------|-----------------|
| 1.50       | 66.67%      | 0.67            |
| 2.00       | 50.00%      | 0.50            |
| 2.50       | 40.00%      | 0.40            |
| 3.00       | 33.33%      | 0.33            |
| 4.00       | 25.00%      | 0.25            |
| 5.00       | 20.00%      | 0.20            |

## Текущие настройки:

- **Размер ставки:** 2.0 USD (из настроек `default_order_size`)
- **Цена:** Автоматически рассчитывается из коэффициента парсера
- **Формула:** Цена = 1 / Коэффициент

## Что можно изменить?

Если нужно изменить размер ставки, отредактируйте `.env` файл:

```env
DEFAULT_ORDER_SIZE=2.0  # Измените на нужное значение
```

Или можно передавать размер через парсер в поле `stake`.

## Пример работы:

1. Парсер отправляет ставку с коэффициентом **2.50**
2. Бот конвертирует: 1 / 2.50 = **0.40**
3. Бот размещает ордер:
   - Цена: **0.40** (40% вероятность)
   - Размер: **2.0 USD**
   - Market: найденный market на Polymarket
   - Side: BUY (покупка)

## Резюме:

✅ **Коэффициент** приходит от парсера (например: 2.50)
✅ **Конвертируется** в вероятность: 1 / коэффициент
✅ **Цена** для Polymarket = вероятность (0.01 - 0.99)
✅ **Размер ставки** = 2.0 USD (из настроек)
✅ Все происходит автоматически
